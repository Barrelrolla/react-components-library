import { Meta, StoryObj } from "@storybook/react";
import { expect, within } from "storybook/test";
import { {{pascalCase name}} } from "./{{pascalCase name}}";
import { availableColors } from "@/types";

const meta: Meta<typeof {{pascalCase name}}> = {
  title: "Components/{{pascalCase name}}",
  tags: ["autodocs"],
  component: {{pascalCase name}},
  decorators: (Story) => (
    <div className="storybookContainer">
      <Story />
    </div>
  ),
  argTypes: {
    children: { name: "text" },
    color: {
      options: availableColors,
      control: { type: "select" },
    },
  },
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement);
    const {{camelCase name}} = canvas.getByText("{{pascalCase name}}");
    await expect({{camelCase name}}, "renders").toBeTruthy();
  },
  render: ({ children, ...rest }) => {
    return <{{pascalCase name}} {...rest}>{children}</{{pascalCase name}}>;
  },
  args: {
    children: "{{name}}",
  },
};

export const Colors: Story = {
  render: ({ ...rest }) => {
    return (
      <div className="flex flex-wrap gap-2">
        {availableColors.map((color) => (
          <{{pascalCase name}} color={color} key={color} {...rest}>
            {color}
          </{{pascalCase name}}>
        ))}
      </div>
    );
  },
  args: {},
  argTypes: {
    color: { table: { disable: true } },
    children: { table: { disable: true } },
  },
};
